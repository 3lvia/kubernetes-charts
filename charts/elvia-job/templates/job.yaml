{{- /* Renders as a one-off Job unless .Values.schedule is set (see cronjob.yaml) */ -}}
{{- if not .Values.schedule }}
apiVersion: batch/v1
kind: Job
metadata:
  {{- include "elvia-job.metadata" . | nindent 2 }}
spec:
  {{- include "elvia-job.jobSpec" . | nindent 2 }}
{{- end }}

