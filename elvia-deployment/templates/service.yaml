apiVersion: v1
kind: Service
metadata:
  name: {{ include "elvia-deployment.fullname" . }}
  labels:
    {{- include "elvia-deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "elvia-deployment.selectorLabels" . | nindent 4 }}
